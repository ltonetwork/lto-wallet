import { TypeLabelPipe } from './type-label.pipe';

describe('TypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
